# LLM Configuration
llm:
  # OpenAI API Key (required for LLM teacher)
  api_key: "sk-..."
  
  # Base URL (optional, e.g. for local models or proxies)
  # base_url: "https://api.openai.com/v1"
  
  # Model name
  model: "gpt-3.5-turbo"
  
  # Initial Bayesian priors (optional)
  # initial_alpha: 1.0
  # initial_beta: 1.0
